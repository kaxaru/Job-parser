-r requirements.txt
pytest>=8
# строгая проверка типов, настройки — mypy.ini подпроекта (свой, не родительский:
# область другая и платформа НЕ фиксируется — стенд исполняется и на Windows, и в Linux)
mypy>=2.3
