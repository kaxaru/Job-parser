# Dev-зависимости: тесты + линтер. (dwh_demo имеет свои requirements.)
-r requirements.txt
pytest>=8
ruff>=0.6
# строгая проверка типов, настройки — mypy.ini
# (комментарий через #, а НЕ через ';' — точка с запятой в requirements это
#  маркер окружения PEP 508, и pip падает на нём: InvalidRequirement)
mypy>=2.3
